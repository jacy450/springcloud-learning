server:
  port: 8901
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jacy450/springcloud-config.git
          username: jacy450
          password: qweasdzxc1104
          clone-on-start: true
          search-paths: '{application}'
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka
